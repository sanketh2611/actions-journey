name: site-check
on: push
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: actions
        uses: actions/chechout@v4
      - name: Read the readme
        run: cat README.md
      - name: Show the Python version
        run: python3 --version
